<div class="collection-page">
  <div class="container">

    {%- if collection.image -%}
      <div style="aspect-ratio:3/1;overflow:hidden;margin-bottom:var(--sp-7);">
        <img
          src="{{ collection.image | image_url: width: 1600 }}"
          alt="{{ collection.image.alt | escape }}"
          loading="eager"
          style="width:100%;height:100%;object-fit:cover;"
          width="1600"
          height="533"
        >
      </div>
    {%- endif -%}

    <div class="collection-header">
      <div class="eyebrow" style="margin-bottom:var(--sp-3);">
        {{ collection.products_count }} {{ collection.products_count | pluralize: 'piece', 'pieces' }}
      </div>
      <h1 class="collection-header__title">{{ collection.title }}</h1>
      {%- if collection.description != blank -%}
        <div class="collection-header__description">{{ collection.description }}</div>
      {%- endif -%}
    </div>

    {%- paginate collection.products by 16 -%}
      <div class="product-grid">
        {%- for product in collection.products -%}
          {%- render 'product-card', product: product -%}
        {%- else -%}
          <p style="grid-column:1/-1;color:var(--fg2);">No products found in this collection.</p>
        {%- endfor -%}
      </div>

      {%- if paginate.pages > 1 -%}
        <nav class="pagination" aria-label="Pagination">
          {%- if paginate.previous -%}
            <a href="{{ paginate.previous.url }}" class="pagination__item" aria-label="Previous page">←</a>
          {%- endif -%}
          {%- for part in paginate.parts -%}
            {%- if part.is_link -%}
              <a href="{{ part.url }}" class="pagination__item">{{ part.title }}</a>
            {%- else -%}
              <span class="pagination__item is-active" aria-current="page">{{ part.title }}</span>
            {%- endif -%}
          {%- endfor -%}
          {%- if paginate.next -%}
            <a href="{{ paginate.next.url }}" class="pagination__item" aria-label="Next page">→</a>
          {%- endif -%}
        </nav>
      {%- endif -%}
    {%- endpaginate -%}

  </div>
</div>

{% schema %}
{
  "name": "Collection",
  "settings": []
}
{% endschema %}
